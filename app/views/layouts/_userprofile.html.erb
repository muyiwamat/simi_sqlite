<div class="span3">
  <div class="well sidebar-nav">
    <ul class="nav nav-list">
      <li class="nav-header"></li>
    </ul>
    <br />
    <p>Preferred Location: <%= current_user.postcode %> <br /></p>
    <%= form_tag("/search", :method => "get") do %>
    <%= label_tag(:search, "Type in search:") %>
    <%= text_field_tag(:search, nil, placeholder: "E.g. AB24 1WU") %>
    <%= submit_tag("Search", class: "btn") %>
    <% end %>
  </div><!--/.well -->
        <%= debug(params) if Rails.env.development? && current_user.email == 'ayo@yahoo.com' %>
</div><!--/span3--> 

