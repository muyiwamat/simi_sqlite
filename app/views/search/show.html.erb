<script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=false"></script>
<script type="text/javascript">

// var geocoder = new google.maps.Geocoder();
// // var address = "AB24%201WU";
// var address = "aberdeen";

// geocoder.geocode( { 'address': address}, function(results, status) {

// if (status == google.maps.GeocoderStatus.OK) {
//     var latitude = results[0].geometry.location.lat();
//     var longitude = results[0].geometry.location.lng();
//     // alert(latitude);
//     } 
// }); 
</script>
<script type="text/javascript">
  var geocoder;
  var map;
  function initialize() {
      // center: new google.maps.LatLng(57.14745,-2.096238), //church location
    geocoder = new google.maps.Geocoder();
    var latlng = new google.maps.LatLng(57.14745,-2.096238);
    var mapOptions = {
      zoom: 8,
      center: latlng,
      mapTypeId: google.maps.MapTypeId.ROADMAP,
      zoomControl:true,
      streetViewControl:false,
      zoomControlOptions: {
        style:google.maps.ZoomControlStyle.SMALL
      },
    }
    map = new google.maps.Map(document.getElementById("map_canvas"), mapOptions);
  }
  function codeAddress() {
    var address = document.getElementById("address").value;
    geocoder.geocode( { 'address': address}, function(results, status) {
      if (status == google.maps.GeocoderStatus.OK) {
        map.setCenter(results[0].geometry.location);
        var marker = new google.maps.Marker({
            map: map,
            position: results[0].geometry.location
        });
      } else {
        alert("Geocode was not successful for the following reason: " + status);
      }
    });
  }
google.maps.event.addDomListener(window, 'load', initialize);
</script>

<% provide(:title, 'Search') %>
<div class="span9">
<div class="hero-unit">
	https://developers.google.com/maps/documentation/javascript/geocoding
	http://railscasts.com/episodes/324-passing-data-to-javascript?view=asciicast
	http://gmaps-samples.googlecode.com/svn/trunk/geocoder/singlegeocode.html
 <div id="map_canvas"></div>
  <div>
    <input id="address" type="textbox" placeholder="Aberdeen">
    <input type="button" value="Encode" onclick="codeAddress()">
    <br />
    <br />
    <p>
    i shld be able to take d params[:id] and pass it to google maps for geocoding and on loading the page completely i show the user the street view and odas...
    </p>
  </div>
</div>
</div><!--/row-->
