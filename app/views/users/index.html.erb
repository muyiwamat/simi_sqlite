<% provide(:title, 'All User Page') %>
<h1>All Users</h1>
<ul class="users">
	<% @users.each do |user| %>
	 <li><%=user.email%>
     <% if current_user.role == "admin" && !current_user?(:user)%>
     <%= link_to " Delete Account", user, method: :delete, data: { confirm: "Are you sure?"}, class: "btn" %>
     <%= link_to " Edit Account", edit_user_path(user), class: "btn" %>
     <% end %>
     </li>
     <br />
  <% end %>
</ul>

